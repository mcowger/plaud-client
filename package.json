{
  "name": "olive-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "lefthook": "^2.1.10",
    "typescript": "^7.0.2"
  }
}
